<Window x:Class="DBHWeeklyReport.App.PreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Xem trước báo cáo tuần"
        Width="1200"
        Height="720"
        WindowStartupLocation="CenterScreen">
    <DockPanel Margin="12">
        <StackPanel DockPanel.Dock="Top" Margin="0,0,0,12">
            <TextBlock Text="{Binding Summary}" FontWeight="SemiBold" FontSize="16" />
            <TextBlock Text="{Binding Preview.ExcelPath}" Foreground="#555" />
        </StackPanel>

        <StackPanel DockPanel.Dock="Bottom"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,12,0,0">
            <Button Width="110" Margin="0,0,8,0" Click="OnWriteClick">Ghi vào Excel</Button>
            <Button Width="110" Click="OnCancelClick">Hủy</Button>
        </StackPanel>

        <DataGrid ItemsSource="{Binding Items}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  HeadersVisibility="Column"
                  RowHeaderWidth="0"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  GridLinesVisibility="Horizontal">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Ngày" Binding="{Binding ReportDate}" Width="100" />
                <DataGridTextColumn Header="Trạng thái" Binding="{Binding Status}" Width="90" />
                <DataGridTextColumn Header="Công việc" Binding="{Binding Task}" Width="220" />
                <DataGridTextColumn Header="Chi tiết" Binding="{Binding Detail}" Width="270" />
                <DataGridTextColumn Header="Ghi chú" Binding="{Binding Memo}" Width="*" />
                <DataGridTextColumn Header="Commit" Binding="{Binding CommitCount}" Width="80" />
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</Window>
